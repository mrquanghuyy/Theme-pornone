"use strict";
const App = (() => {
  const e = [];
  return {
    modules: e,
    initialize: function () {
      e.forEach((e) => {
        $(document).find(e.wrapFor).length && e.initialize();
      });
    },
  };
})();
window.App = App;
const loadItems = function () {
    const e = jQuery.Deferred(),
      t = login_url,
      i = logged
        ? '<input  class="vert-bottom grayplace inline-block float-left mr-1 h-[32px] w-[185px] p-1 " type="text" maxlength="64" id="newplaylistname" placeholder="' +
          infoNewPlayl +
          '"><img class="vert-bottom mr-1 float-left" src="/images/check32.png"  width="32" height="32" alt="NewPlaylist" onclick="newplaylist()"><img class="vert-bottom float-left" src="/images/cancel32n.png" width="32" height="32" alt="NewPlaylist" onclick="$(\'#newplaylistname\').val(\'\');" >'
        : '<a href="' +
          t +
          '" data-backto="' +
          encodeURIComponent(location.href) +
          '">' +
          infoLoginTo +
          ".</a>",
      s = {};
    if (
      ((s.NewPlaylist = {
        name: i,
        isHtmlName: !0,
      }),
      "undefined" != typeof lists)
    )
      for (const e in lists)
        lists[e].hasOwnProperty("username") ||
          (s[`id${lists[e].id}`] = {
            name: lists[e].title,
          });
    return (
      setTimeout(() => {
        e.resolve(s);
      }, 200),
      e.promise()
    );
  },
  favsTitle = function (e) {
    return "undefined" != typeof vafids && vafids.includes(e);
  },
  favsAddDell = function (e, t) {
    "undefined" != typeof vafids
      ? vafids.includes(e)
        ? (favorite(e, 1), (vafids = vafids.replace(`,${e}`, "")))
        : ((vafids += `,${e}`), favorite(e, 0))
      : favorite(e, 0);
  };

function showplaylist() {
  const e = $(".right-menu");
  void 0 !== e &&
    (e.is(":hidden")
      ? (window.listq &&
          (lists && Object.keys(lists).length > 0
            ? playtab("tabplaylist")
            : playtab("tabqueue")),
        e.show())
      : e.hide());
}

function bioShowHide(e, t, i) {
  const s = $(e),
    o = $("#info-bio"),
    l = $("#info-details");
  return (
    o.hasClass("h-27")
      ? (o.removeClass("h-27").addClass("h-full"),
        l.removeClass("hidden").addClass("h-full"),
        s.html(i))
      : (o.removeClass("h-full").addClass("h-27"),
        l.removeClass("h-full").addClass("hidden"),
        s.html(t)),
    !1
  );
}

function showhide(e) {
  -1 !== e.indexOf("player-menu") &&
    ($(".player-menu-comments").hide(),
    $(".player-menu-images").hide(),
    $(".player-menu-videos").hide());
  const t = $(e);
  (void 0 !== t && (t.is(":hidden") ? t.show() : t.hide()),
    ".info-details" == e &&
      (t.is(":hidden")
        ? ($(".show-hide-text").html("Show full bio"),
          $(".star-descr").css("height", "60px"))
        : ($(".star-descr").css("height", "100%"),
          $(".show-hide-text").html("Hide full bio"))),
    ".player-menu-info" === e &&
      $(".sidebar-box").css(
        "max-height",
        $(".sidebar-box").find("p").outerHeight() +
          $(".read-more").outerHeight() +
          57,
      ));
}

function movescrollpadding(e, t) {
  ($.browser.webkit || $.browser.msedge || $.browser.msie) &&
    (e > 6
      ? $(t).css("padding-right", "0px")
      : $(t).css("padding-right", "17px"));
}

function newplaylist() {
  const e = $("#newplaylistname").val();
  Object.keys(lists).length >= 128
    ? showtempinfo(infoNoMore, 3e3, !1)
    : e.length > 3
      ? $.get(
          "/ajax/videoget.php",
          {
            insertplay: us,
            title: e,
            newlist: listq[11],
            lang_id: lang_id,
            lang_name: lang_name,
          },
          (t) => {
            if ("error" !== t) {
              const i = JSON.parse(t);
              ((lists[i.id] = i),
                lists[i.id].status++,
                addnewplaylist(e, i.id),
                movescrollpadding(Object.keys(lists).length, "#play_list"),
                $("#labelplaylist").html(
                  `${Object.keys(lists).length} Playlists`,
                ),
                showtempinfo(
                  infoNewPlayl + ` ${e} ` + infoCreated,
                  3e3,
                  "#F0FAEB",
                ),
                $(".context-menu-active").contextMenu("hide"),
                $("#newplaylistname").val(""));
            }
          },
        )
      : showtempinfo(infoMust4, 3e3, !1);
}

function showpopup(e, t, i) {
  const s = document.createElement("div");
  (s.setAttribute("style", "position:relative;top:-110%;background-color:red;"),
    (s.innerHTML = t),
    setTimeout(() => {
      s.parentNode.removeChild(s);
    }, i),
    e.appendChild(s));
}

function showtempinfo(e, t, i) {
  ($("#temp-info-h4").html(e),
    i
      ? ($("#temp-info-color").removeClass("bg-red1").addClass("bg-green-200"),
        $("#temp-info-playlist-close").hide(),
        $("#temp-info-playlist-done").show())
      : ($("#temp-info-color").removeClass("bg-green-200").addClass("bg-red1"),
        $("#temp-info-playlist-close").show(),
        $("#temp-info-playlist-done").hide()),
    -1 == i &&
      ($("#temp-info-color").removeClass("bg-green-200").addClass("bg-red1"),
      $(".infoIcon").hide(),
      $("#temp-info-h4").addClass("text-white")),
    $(".temp-info-fixed").show(),
    setTimeout(() => {
      ($(".temp-info-fixed").hide(),
        $("#temp-info-color").removeClass("bg-red1").addClass("bg-green-200"),
        $(".infoIcon").show(),
        $("#temp-info-h4").removeClass("text-white"));
    }, t));
}

function playshuffle() {
  let e = "";
  switch (listq[13]) {
    case 1:
      var t = JSON.parse(rcookie("qplaylist")) || [];
      if (t.length > 0) {
        ((e = `/bbw/wife/${(e = t[Math.floor(Math.random() * t.length)])}/?que=${t.indexOf(e)}`),
          (document.location = e));
      }
      break;
    case 2:
      for (var i in lists) e += `,${lists[i].list},`;
      if ((s = e.split(",")).length > 3) {
        do {
          e = s[Math.floor(Math.random() * s.length) + 1];
        } while ("" === e || "null" === e);
        for (var i in lists) {
          let t = ~lists[i].list.indexOf(e);
          if (t || lists[i].list == e) {
            ((e = `/bbw/wife/${e}/?playlist=${i}&v=${(t = (s = lists[i].list.split(",")).indexOf(e) + 1)}`),
              (document.location = e));
            break;
          }
        }
      }
      break;
    case 3:
      var s,
        o = 0;
      if (lists[listq[3]].status > 1)
        e = (s = lists[listq[3]].list.split(","))[
          (o = Math.floor(Math.random() * (lists[listq[3]].status - 1)))
        ];
      else e = lists[listq[3]];
      ((e = `/bbw/wife/${e}/?playlist=${listq[3]}&v=${o + 1}`),
        (document.location = e));
      break;
    default:
      return !1;
  }
}

function changeCSSimages(e) {
  "undefined" != typeof imgpath &&
    ($("#footerlogo").attr("src", `${imgpath}/images/logo-grey${e}.svg`),
    $(".logosvg").attr("src", `${imgpath}/images/svg/logo${e}.svg`),
    $("#logosimple").attr("src", `${imgpath}/images/svg/logosimple${e}.svg`),
    $(".rtalabel").attr("src", `${imgpath}/images/rta${e}.gif`));
}

function shortenString(e, t) {
  const i = e.substr(0, t);
  return /^\S/.test(e.substr(t)) ? `${i.replace(/\s+\S*$/, "")}...` : i;
}

function addtoplaylist(e, t, i, s, o, l) {
  const a = t || document.getElementById("play_listqueue");
  let n = `<div class="w-full p-2 border-b-2 border-dashed border-gray-200 dark:border-black-33 hover:bg-white-six hover:dark:bg-black-d1 ${l && l === i ? "selekt" : ""}">\n                  <div class="flex justify-start items-center">\n                    <a class="basis-1/12 flex-none mr-2" href="${e.url}${s}${i}">\n                        ${!1 !== l && l == i ? '<img src="/images/svg/play.svg" ></img>' : parseInt(i) + 1} \n                    </a>\n                    <a class="basis-1/4 flex-none mr-2" href="${e.url}${s}${i}">\n                      <img class="w-[100px]" width="100" height="59" ids="${parseInt(e.id)}" src="${e.thumbsmall}">\n                    </a>\n                    <div class="basis-2/3 pr-2">\n                      <a class="w-full block text-f15 break-all" href="${e.url}${s}${i}">${shortenString(e.title, 47)}</a>\n                      <span class="text-f13">by </span>\n                      <span class="text-f13 font-semibold">${shortenString(e.username, 17)}</span>\n                    </div>`;
  (listq[3] && void 0 !== lists[listq[3]].username
    ? (n += "</div></div>")
    : (n += `<span class="ml-auto"  data-toggle="tooltip" data-placement="top" data-original-title="Delete" data-container="body">\n                        <a href="#" onclick="${t && "play_listlist" === t.id ? "deleteplayvideo(" : "deletefromqueue("}${e.id},$(this.parentElement.parentElement.parentElement));return false;"">\n                            <img src="${imgpath}/images/delete.png" alt="Delete from playlist" width="12" height="16">\n                        </a>\n                    </span>\n                  </div>\n              </div>`),
    (a.innerHTML += n));
}

function addtoplaylistNextPrev(e, t, i, s, o) {
  let l = `<div class="w-full p-2 border-b-2 border-dashed border-gray-200 dark:border-black-33 hover:bg-white-six hover:dark:bg-black-d1 text-center">\n\n<span onclick="displayplaylist.call(this,event,${e},'play_list',${o},${0 !== i && i});" class="inline-flex py-2 px-4 border border-light-red-two shadow-sm text-white-two font-semibold rounded bg-light-red-two hover:bg-white hover:text-light-red cursor-pointer">\n${s ? infoShowPrev : infoShowMore}\n</span>\n\n                 </div>`;
  (t || document.getElementById("play_listqueue")).innerHTML += l;
}

function addnewplaylist(e, t) {
  const i = document.getElementById("play_list"),
    s = `<div class="w-full p-2 border-b-2 border-dashed border-gray-200 hover:bg-white-six">\n                  <div class="flex justify-start items-center">\n                    <a class="basis-1/12 flex-none mr-2" href="/">\n                        ${Object.keys(lists).length}\n                    </a>\n                    <a class="basis-1/4 flex-none mr-2 relative group" href="/">\n                      <img class="w-[100px]" width="100" height="59" id="listimg${t}" src="${thumbpath}t/${listq[11] % 100}/${listq[11]}/d${listq[12]}.jpg" loading="lazy">\n                      <thumbnail-overlay class="transition-opacity duration-300 ease-in opacity-70 group-hover:opacity-0 flex flex-col items-center justify-center absolute top-0 right-0 bg-black-dark h-full w-6/12">\n                        <thumbnail-icon>\n                            <img src="/images/playlist-thumb-big.png" width="20" height="18" alt="playlist thumb">\n                        </thumbnail-icon>\n                      </thumbnail-overlay>\n                      <thumbnail-overlay-all class="transition-opacity duration-300 ease-in opacity-0 group-hover:opacity-70 flex flex-col items-center justify-center  absolute top-0 left-0 uppercase bg-black-dark h-full w-full">\n                        <img src="/images/play-all-big.png" class="playalllist" width="10" height="12" alt="play all">\n                        <span class="mt-2 text-white text-f11">play all</span>\n                      </thumbnail-overlay-all>\n                    </a>\n                    <div class="basis-2/3 pr-2">\n                      <a class="w-full block text-f15 break-all" onmouseup="displayplaylist.call(this,event,${t},'play_list')" href="javascript:void(0);">\n                        <span class="pointpIcon inline-block h-6 w-6  bg-gray-400 hover:bg-gray-500 fill-current group-hover:rotate-0 rotate-180 transform"  ></span>\n                        ${shortenString(e, 47)}\n                      </a>\n                      <span  onclick="displayplaylist.call(this,event,${t},'play_list')" class="text-f13 font-semibold cursor-pointer">1 video</span>\n                    </div>\n                    <span class="ml-auto" data-toggle="tooltip" data-placement="top" data-original-title="Delete" data-container="body">\n                        <a href="#" onclick="deleteplaylist($(this.parentElement.parentElement.parentElement),${t});return false;">\n                            <img src="${imgpath}/images/delete.png" alt="Delete from playlist" width="12" height="16">\n                        </a>\n                    </span>\n                  </div>\n              </div>`;
  i.innerHTML += s;
}

function displayplaylist(e, t, i, s, o = !1) {
  if (lists[t]) {
    const e =
        new URLSearchParams(window.location.href.split("?")[1]).get("list") ||
        !1,
      i = (lists[t].list.match(/,/g) || []).length;
    let l = lists[t].list.split(","),
      a = !1,
      n = !1;
    (i > 99 &&
      ((l = l.slice(100 * o, 100 * o + 100)),
      o > 0 && (a = o - 1 == 0 || o - 1),
      i > 100 * (o + 1) && (n = o + 1)),
      $.get(
        "/ajax/videoget.php",
        {
          jsplaylist: l.join(),
          lang_id: lang_id,
          lang_name: lang_name,
        },
        (i) => {
          if ("error" != i) {
            ((listq[3] = t), $("#btnback").show());
            const d = JSON.parse(i);
            ($("#labelplaylistlist").html(
              `${lists[listq[3]].title.substring(0, 40)} ${1 + 100 * o}/${d.length + 100 * o} of ${lists[listq[3]].status}`,
            ),
              void 0 === lists[listq[3]].username &&
                $("#labelplaylistlistuser").hide());
            const c = document.getElementById("play_listlist");
            ((c.innerHTML = ""),
              a && addtoplaylistNextPrev(t, c, a ? o - 1 : 0, !0, s));
            for (var l = 0; l < d.length; l++)
              if (
                (addtoplaylist(
                  d[l],
                  c,
                  l + 100 * o,
                  `/?playlist=${listq[3]}&v=`,
                  l,
                  e == t && s,
                ),
                s === l + 100 * o && l + 1 < d.length)
              ) {
                if ("#" == d[l + 1].url) {
                  s++;
                  continue;
                }
                if (void 0 === r) {
                  var r =
                    d[l + 1].url +
                    `?playlist=${listq[3]}&v=` +
                    (l + 1 + 100 * o);
                  d[l + 1].title;
                } else
                  ((r =
                    d[l + 1].url +
                    `?playlist=${listq[3]}&v=` +
                    (l + 1 + 100 * o)),
                    d[l + 1].title);
                setvideonext(
                  d[l + 1].url + `?playlist=${listq[3]}&v=` + (l + 1 + 100 * o),
                );
              } else if (l + 1 === d.length) {
                if (void 0 === r)
                  ((r = d[0].url + `?playlist=${listq[3]}&v=0`), d[0].title);
                else ((r = d[0].url + `?playlist=${listq[3]}&v=0`), d[0].title);
                setvideonext(d[0].url + `?playlist=${listq[3]}&v=0`);
              }
            (n && addtoplaylistNextPrev(t, c, n, !1, s),
              !1 === a && 0 === o
                ? backtolist()
                : c.scrollIntoView(
                    {
                      behavior: "smooth",
                    },
                    !0,
                  ));
          }
        },
      ));
  }
}

function playtab(e) {
  if (
    ($("#tabqueue").removeClass("border-b-2"),
    $("#tabplaylist").removeClass("border-b-2"),
    $(`#${e}`).addClass("border-b-2"),
    $("#play_listlist").hide(),
    "tabplaylist" == e)
  ) {
    if (
      $("#play_listqueue").is(":hidden") &&
      $("#play_listlist").html().length > 100
    )
      return (backtolist(), !0);
    ($("#play_listqueue").hide(),
      $("#labelplaylist").show(),
      $("#labelplaylistlist").show(),
      $("#labelplaylistqueue").hide(),
      window.listq && listq[2]
        ? ((listq[13] = 2),
          $("#labelplaylistlist").hide(),
          $("#play_list").show())
        : ((listq[13] = 3),
          $("#labelplaylist").hide(),
          $("#labelplaylistlist").show(),
          $("#btnback").show(),
          $("#play_list").hide(),
          $("#play_listlist").show()));
  } else
    ($("#play_list").hide(),
      $("#play_listqueue").show(),
      $("#btnundo").hide(),
      $("#btnback").hide(),
      $("#labelplaylist").hide(),
      $("#labelplaylistlist").hide(),
      $("#labelplaylistqueue").show(),
      (listq[13] = 1));
}

function backtolist() {
  $("#play_list").is(":visible")
    ? ($("#labelplaylistlist").show(),
      $("#play_listlist").show(),
      (listq[13] = 3),
      $("#play_list").hide(),
      $("#labelplaylist").hide())
    : ($("#labelplaylistlist").hide(),
      $("#labelplaylistlistuser").hide(),
      $("#play_listlist").hide(),
      $("#play_list").show(),
      (listq[13] = 2),
      $("#labelplaylist").show(),
      Object.keys(lists).length > 6 &&
        scrolltoclass("#play_list", ".selektplist"));
}

function scrolltoclass(e, t) {
  const i = $(e),
    s = $(t);
  i.length &&
    s.length &&
    i.animate({
      scrollTop: s.offset().top - i.offset().top + i.scrollTop(),
    });
}

function fillqueuelist() {
  const e = rcookie("qplaylist") || [],
    t = document.getElementById("play_listqueue"),
    i =
      new URLSearchParams(window.location.href.split("?")[1]).get("que") || !1;
  (null != t && e.length > 2
    ? $.get(
        "/ajax/videoget.php",
        {
          playlist: e,
          lang_id: lang_id,
          lang_name: lang_name,
        },
        (e) => {
          if ("error" != e) {
            const l = t.getElementsByClassName("listvideo");
            for (; l[0]; ) l[0].parentNode.removeChild(l[0]);
            const a = JSON.parse(e);
            for (var s = 0; s < a.length; s++)
              if (
                (addtoplaylist(a[s], t, s, "?que=", listq[4], i),
                !1 !== i &&
                  "function" == typeof setvideonext &&
                  i == s &&
                  s + 1 < a.length)
              ) {
                if (void 0 === o) {
                  var o = a[s + 1].url + "?que=" + (s + 1);
                  a[s + 1].title;
                } else ((o = a[s + 1].url + "?que=" + (s + 1)), a[s + 1].title);
                setvideonext(a[s + 1].url + "?que=" + (s + 1));
              }
            (s > 6 && scrolltoclass("#play_listqueue", ".selekt"),
              $("#labelplaylistqueue").html(
                listq[4] ? `${listq[4]}/${s} Videos` : `${s} Videos`,
              ));
          }
        },
      )
    : "undefined" != typeof infoNoVideoQueue &&
      $("#labelplaylistqueue").html(infoNoVideoQueue),
    "function" == typeof setvideonext && setvideonext());
}

function addtoqueue(e, t, i) {
  const s = rcookie("qplaylist") || [];
  if (s.length > 2 && s.split(",").length >= 64)
    showtempinfo(infoCantAddQ, 3e3, !1);
  else {
    const i = addtolist(parseInt(e));
    null !== i
      ? $.get(
          "/ajax/videoget.php",
          {
            id: e,
            lang_id: lang_id,
            lang_name: lang_name,
          },
          (e) => {
            if ("error" != e) {
              const s = JSON.parse(e);
              (showtempinfo(infoAddedQ, 3e3, "#F0FAEB"),
                addtoplaylist(s[0], null, t ? i + 1 : i, "?que=", i),
                $("#labelplaylistqueue").html(`${i + 1} ` + infoVideos),
                movescrollpadding(i + 1, "#play_listqueue"));
            }
          },
        )
      : showtempinfo(infoAlreadyQ, 2e3, !1);
  }
}

function undoplaylistlist() {
  (listq[9].show(),
    $("#btnundo").attr("onclick", "javascript:void(0);").hide(),
    $.get(
      "/ajax/videoget.php",
      {
        playup: listq[6],
        playid: lists[listq[3]].id,
        lang_id: lang_id,
        lang_name: lang_name,
      },
      (e) => {
        const t = $(`#listcount${listq[3]}`);
        (lists[listq[3]].status++, t.html(`${lists[listq[3]].status} videos`));
        const i = $("#labelplaylistlist").html();
        ($("#labelplaylistlist").html(
          i.substring(0, i.indexOf("/") + 1) + lists[listq[3]].status,
        ),
          "error" != e && showtempinfo(infoUndoV, 3e3, "#F0FAEB"));
      },
    ));
}

function undoplaylist() {
  (listq[9].show(),
    $("#btnundo").attr("onclick", "javascript:void(0);").hide());
  const e = Object.keys(lists).length + 1;
  ($("#labelplaylist").html(`${e} Playlists`),
    $.get(
      "/ajax/videoget.php",
      {
        undodellist: listq[6],
        lang_id: lang_id,
        lang_name: lang_name,
      },
      (e) => {
        "error" != e && showtempinfo(infoUndoPl, 3e3, "#F0FAEB");
      },
    ));
}

function playlistdelete() {
  if (listq[6] && "video" === listq[7]) {
    ($("#btnundo").attr("onclick", "undoplaylistlist()").show(),
      (lists[listq[3]].list = lists[listq[3]].list.replace(
        `,${listq[6].toString()}`,
        "",
      )),
      (lists[listq[3]].list = lists[listq[3]].list.replace(
        `${listq[6].toString()},`,
        "",
      )));
    const e = $(`#listcount${listq[3]}`);
    (lists[listq[3]].status--, e.html(`${lists[listq[3]].status} videos`));
    const t = $("#labelplaylistlist").html();
    ($("#labelplaylistlist").html(
      t.substring(0, t.indexOf("/") + 1) + lists[listq[3]].status,
    ),
      $.get(
        "/ajax/videoget.php",
        {
          deletevid: listq[6],
          playid: listq[3],
          lang_id: lang_id,
          lang_name: lang_name,
        },
        (e) => {
          "error" != e
            ? showtempinfo(infoDELETED, 1e3, "#F0FAEB")
            : showtempinfo(infoERROR, 1e3, !1);
        },
      ),
      lists[listq[3]].status <= 0 &&
        $.get(
          "/ajax/videoget.php",
          {
            deleteplay: listq[3],
            lang_id: lang_id,
            lang_name: lang_name,
          },
          (e) => {
            "error" != e
              ? showtempinfo(infoDELETED + " " + infoPlaylist, 1e3, "#F0FAEB")
              : showtempinfo(infoERROR, 1e3, !1);
          },
        ));
  }
  if (listq[6] && "play" === listq[7]) {
    ($("#btnundo").attr("onclick", "undoplaylist()").show(),
      delete lists[listq[6]]);
    const e = Object.keys(lists).length;
    ($("#labelplaylist").html(`${e} Playlists`),
      $.get(
        "/ajax/videoget.php",
        {
          deleteplay: listq[6],
          lang_id: lang_id,
          lang_name: lang_name,
        },
        (e) => {
          "error" != e
            ? showtempinfo(infoDELETED, 1e3, "#F0FAEB")
            : showtempinfo(infoERROR, 1e3, !1);
        },
      ));
  }
}

function canceldelete(e) {
  (e.prev().show(), $("#playlistdelete").hide());
}

function deleteplaylist(e, t) {
  ((listq[6] = t),
    (listq[7] = "play"),
    (listq[8] = e.index()),
    (listq[9] = e),
    e.hide(),
    e.after($("#playlistdelete")),
    $("#playlistdelete").show());
}

function deleteplayvideo(e, t) {
  ((listq[6] = e),
    (listq[7] = "video"),
    (listq[8] = t.index()),
    (listq[9] = t),
    t.hide(),
    t.after($("#playlistdelete")),
    $("#playlistdelete").show());
}

function addtolist(e) {
  const t = JSON.parse(rcookie("qplaylist")) || [];
  if (-1 == t.indexOf(e)) {
    t.push(e);
    const i = JSON.stringify(t);
    return (setcookie("qplaylist", i, 0.5), i.split(",").length - 1);
  }
  return null;
}

function undotolist(e) {
  ($("#btnundo").attr("onclick", "javascript:void(0);").hide(),
    listq[10].show(),
    addtolist(parseInt(e)),
    showtempinfo(infoUndoQ, 3e3, "#F0FAEB"),
    isNaN(parseInt($("#labelplaylistqueue").html().charAt(0)))
      ? $("#labelplaylistqueue").html("1 Video")
      : $("#labelplaylistqueue").html(
          `${parseInt($("#labelplaylistqueue").html().charAt(0)) + 1} Videos`,
        ));
}

function deletefromlist(e) {
  const t = JSON.parse(rcookie("qplaylist")) || [],
    i = t.indexOf(e);
  if (-1 != i) {
    (t.splice(i, 1),
      setcookie("qplaylist", JSON.stringify(t), !1, 10),
      $("#btnundo").attr("onclick", `undotolist(${e})`).show());
    const s = t.length;
    return (
      s > 0
        ? $("#labelplaylistqueue").html(`${s} Videos`)
        : $("#labelplaylistqueue").html(infoNoVideoQueue),
      !0
    );
  }
  return null;
}

function deletefromqueue(e, t) {
  ((listq[6] = e), (listq[10] = t), t.hide(), deletefromlist(e));
}

function initDownload(e, t, i, s) {
  const o = renderTemplate("#downloadmodal");
  (o.find("#msize").text(`${s} - ${i} Quality`),
    logged
      ? o
          .find(".down-now")
          .attr("href", e)
          .on("click", (e) => {
            setTimeout(() => {
              Popup.close();
            }, 500);
          })
      : o
          .find(".down-now")
          .attr("href", login_url)
          .data("backto", encodeURIComponent(location.href))
          .text("Please login/register to download"),
    Popup.open(o));
}

function downloadVideo(e) {
  if (!logged) return initlogin("Login in order to download files: ");
  alert(`Downloading video for ${e}`);
}

function vote(e, t) {
  // WordPress core theme: inc/ajax.php → ratemovie (giống media.js voteMovie)
  var postId =
    typeof video !== "undefined" && video
      ? video
      : typeof ophimPostId !== "undefined"
      ? ophimPostId
      : null;
  if (!postId) {
    if (typeof showtempinfo === "function") {
      showtempinfo(
        typeof infoERROR !== "undefined" ? infoERROR : "Thiếu ID bài viết",
        3e3,
        !1
      );
    }
    return;
  }
  var u =
    typeof ajaxUrl !== "undefined" ? ajaxUrl : "/wp-admin/admin-ajax.php";
  $.post(
    u,
    {
      action: "ratemovie",
      rating: e,
      postid: postId,
    },
    function (resp) {
      if (!resp || resp.status !== "success") return;
      $(".voteupcount").html(resp.like_count);
      $(".votedowncount").html(resp.dislike_count);
      if (typeof uservote !== "undefined") {
        uservote = e;
      }
    },
    "json"
  ).fail(function () {
    if (typeof showtempinfo === "function") {
      showtempinfo(
        typeof infoERROR !== "undefined" ? infoERROR : "Lỗi kết nối",
        3e3,
        !1
      );
    }
  });
}

function commentvote(e, t) {
  $.post(
    "/ajax/commentvote.php",
    {
      comment: e,
      vote: parseInt(t),
    },
    (i) => {
      if ("error" != i) {
        const i = $(`#commlikes${e}`),
          s = i.html().split(" ", 1);
        parseInt(t) > 0
          ? (classElmnt(`comvoteimg${e}`, "text-red1"),
            i.html(`${parseInt(s) + 1} ${infoLikes}`),
            $(`#comvoteimg${e}`).attr("onclick", `commentvote(${e},0)`))
          : (classElmnt(`comvoteimg${e}`, "text-red1"),
            i.html(`${parseInt(s) - 1} ${infoLikes}`),
            $(`#comvoteimg${e}`).attr("onclick", `commentvote(${e},1)`));
      }
    },
  );
}

function favorite(e, t) {
  logged
    ? void 0 !== e
      ? $.post(
          "/ajax/favorite.php",
          {
            video: e,
            remove: void 0 !== t && 1 === t ? 1 : 0,
          },
          (e) => {
            "ok" == e &&
              showtempinfo(
                void 0 !== t && 1 === t ? infoRemVaf : infoAddVaf,
                3e3,
                "#F0FAEB",
              );
          },
        )
      : $.post(
          "/ajax/favorite.php",
          {
            video: video,
            remove: favorited + 0,
          },
          (e) => {
            "ok" == e &&
              (favorited
                ? (showtempinfo(infoRemVaf, 3e3, "#F0FAEB"),
                  classElmnt("favorit", "text-red1"),
                  classElmnt("favorit", "dark:text-warm-grey"),
                  classElmnt("favorit", "text-black1"),
                  classElmnt("favoritmob", "text-red1"),
                  classElmnt("favoritmob", "dark:text-warm-grey"),
                  classElmnt("favoritmob", "text-black1"),
                  $("#favorit")
                    .parent()
                    .attr("data-original-title", "Add to Favorites"),
                  $("#favoritmob")
                    .parent()
                    .attr("data-original-title", "Add to Favorites"))
                : (showtempinfo(infoAddVaf, 3e3, "#F0FAEB"),
                  classElmnt("favorit", "text-red1"),
                  classElmnt("favorit", "dark:text-warm-grey"),
                  classElmnt("favorit", "text-black1"),
                  classElmnt("favoritmob", "text-red1"),
                  classElmnt("favoritmob", "dark:text-warm-grey"),
                  classElmnt("favoritmob", "text-black1"),
                  $("#favorit")
                    .parent()
                    .attr("data-original-title", "Remove from Favorites"),
                  $("#favoritmob")
                    .parent()
                    .attr("data-original-title", "Remove from Favorites")),
              (favorited = !favorited));
          },
        )
    : (backtoCookie(window.location.href), (window.location = login_url));
}

function subscribestar(e, t) {
  var i = $("#star-" + e);
  logged
    ? $.post(
        "/ajax/subscribestar.php",
        {
          starid: e || "",
          remove: t || subscribedstar + 0,
        },
        (t) => {
          "ok" == t &&
            (subscribedstar
              ? (i.attr("onclick", "subscribestar(" + e + "); return false;"),
                i.removeClass("bg-green1"),
                i.addClass("bg-light-red-two"),
                i.find(".sbs").addClass("block").removeClass("hidden"),
                i
                  .find(".sbsd")
                  .addClass("hidden")
                  .removeClass("block")
                  .removeClass("group-hover:hidden"),
                i.find(".usbs").removeClass("group-hover:block"))
              : (i.attr("onclick", "subscribestar(" + e + ",1); return false;"),
                i.addClass("bg-green1").addClass("hover:bg-light-red-two"),
                i.removeClass("bg-light-red-two"),
                i.find(".sbs").removeClass("block").addClass("hidden"),
                i
                  .find(".sbsd")
                  .removeClass("hidden")
                  .addClass("block")
                  .addClass("group-hover:hidden"),
                i.find(".usbs").addClass("group-hover:block")),
            (subscribedstar = !subscribedstar));
        },
      )
    : (backtoCookie(window.location.href), (window.location = login_url));
}

function subscribeStarRemove(e) {
  var t = $("#star-" + e);
  logged
    ? $.post(
        "/ajax/subscribestar.php",
        {
          starid: e || "",
          remove: 1,
        },
        (e) => {
          "ok" == e &&
            (t.removeClass("bg-green1").addClass("bg-light-red-two"),
            t
              .find(".sbsd")
              .addClass("hidden")
              .removeClass("block")
              .removeClass("group-hover:hidden"),
            t
              .find(".usbs")
              .addClass("block")
              .removeClass("hidden")
              .removeClass("group-hover:block"));
        },
      )
    : (backtoCookie(window.location.href), (window.location = login_url));
}

function subscribe(e, t) {
  if (logged)
    ("undefined" == typeof subscribed && (subscribed = subusers[e].subscribed),
      $.post(
        "/ajax/subscribe.php",
        {
          userid: e || uploaderid,
          remove: t || subscribed + 0,
        },
        (t) => {
          "ok" == t &&
            (subscribed
              ? $(".subscribe").removeClass("subscribd")
              : $(".subscribe").addClass("subscribd"),
            "undefined" != typeof subusers &&
              (subusers[e].subscribed = !subscribed),
            (subscribed = !subscribed));
        },
      ));
  else {
    if (!ismobile) return initLoginReg("register");
    (backtoCookie(window.location.href), (window.location = login_url));
  }
}

function subscribenew(e) {
  if (logged) {
    let t = $(e.target).hasClass("subscribd");
    ($.post(
      "/ajax/subscribe.php",
      {
        userid: $(e.target).attr("data-user"),
        remove: t + 0,
        isrow: isrow + 0,
        ismobile: ismobile,
        uid: uid + 0,
        page: page + 0,
      },
      (e) => {
        "ok" == e && (t = !t);
      },
    ),
      t
        ? $(e.target).removeClass("subscribd")
        : $(e.target).addClass("subscribd"));
  } else {
    if (!ismobile) return initLoginReg("register");
    (backtoCookie(window.location.href), (window.location = login_url));
  }
}

function subscribeUser(e) {
  if (logged) {
    var t = subusers[e].subscribed;
    $.post(
      "/ajax/subscribe.php",
      {
        userid: e,
        remove: t + 0,
      },
      (i) => {
        if ("ok" == i) {
          var s = $("#subscribe_" + e);
          (t
            ? (s.removeClass("bg-green1"),
              s.addClass("bg-light-red-two"),
              s.find(".sbs").addClass("block").removeClass("hidden"),
              s
                .find(".sbsd")
                .addClass("hidden")
                .removeClass("block")
                .removeClass("group-hover:hidden"),
              s.find(".usbs").removeClass("group-hover:block"))
            : (s.addClass("bg-green1").addClass("hover:bg-light-red-two"),
              s.removeClass("bg-light-red-two"),
              s.find(".sbs").removeClass("block").addClass("hidden"),
              s
                .find(".sbsd")
                .removeClass("hidden")
                .addClass("block")
                .addClass("group-hover:hidden"),
              s.find(".usbs").addClass("group-hover:block")),
            "undefined" != typeof subusers && (subusers[e].subscribed = !t),
            (t = !t),
            (subusers[e].subscribed = t));
        }
      },
    );
  } else (backtoCookie(window.location.href), (window.location = login_url));
}

function backtoCookie(e) {
  var t = new Date();
  t.setTime(t.getTime() + 3e5);
  var i = "; expires=" + t.toUTCString();
  document.cookie = "backto=" + encodeURIComponent(e) + ";" + i + "; path=/";
}

function commentreplyshow(e, t) {
  ((listq[14] = t),
    e.after($("#commentreply-row")),
    classElmnt("commentreply-row", "hidden"),
    classElmnt("commentreply-row", "md:block"),
    classElmnt("commentreply-row", "flex"));
}

function commentreply() {
  const e = $("#textfieldreply").val();
  !e || e.length < 4
    ? alert(infoEnterSom)
    : (logged ||
        (backtoCookie(window.location.href), (window.location = login_url)),
      classElmnt("commentreply-row", "hidden"),
      $.post(
        "/ajax/comment.php",
        {
          comment: e,
          videoid: video,
          reply: listq[14],
        },
        (e) => {
          (update_comments(1), $("#textfieldreply").val(""));
        },
      ));
}

function commentar() {
  const e = $("#textfield")
    .val()
    .replace(/['`"]/g, "")
    .replace(/[^a-zA-Z0-9:?!.,-]/g, " ");
  !e || e.length < 4
    ? alert(infoEnterSom)
    : (logged ||
        (backtoCookie(window.location.href), (window.location = login_url)),
      loading_comments(),
      $.post(
        "/ajax/comment.php",
        {
          comment: e,
          videoid: video,
        },
        (e) => {
          ("ok" != e ? showtempinfo(e, 5e3, -1) : update_comments(1),
            $("#textfield").val(""));
        },
      ));
}

function loading_comments() {}

function update_comments(e, t) {
  ($.get(
    "/ajax/comments.php",
    {
      videoid: video,
      page: e,
      mobile: ismobile,
      lang: lang_name,
      locale: lang_locale,
      sortpopular: void 0 !== t ? t : "false",
    },
    (e) => {
      ($("#comment-wrap").empty().html(e),
        "undefined" != typeof videocomments &&
          videocomments &&
          $("#comm-id").empty().text(`Comments (${videocomments})`));
    },
  ),
    loading_comments());
}

function get_comments(e, t) {
  $("#js-comments").length &&
    ($.get(
      "/ajax/commentsget.php",
      {
        videoid: video,
        page: e,
        mobile: ismobile,
        sortpopular: void 0 !== t ? t : "false",
      },
      (e) => {
        ($("#comment-wrap").empty().html(e),
          "undefined" != typeof videocomments &&
            videocomments &&
            $("#comm-id").empty().text(`Comments (${videocomments})`));
      },
    ),
    loading_comments());
}

function remove_comment(e) {
  if (!logged) return initlogin();
  ($(`#cmnt_${e}`).hide("slow"),
    $.get(
      "/ajax/removecomment.php",
      {
        comment: e,
        videoid: video,
      },
      (t) => {
        "ok" === t
          ? ismobile &&
            (videocomments--,
            (document.getElementById("comm-id").innerHTML =
              `${videocomments} Comments`),
            (document.getElementById("commnum-id").style.display = "inherit"),
            (document.getElementById("commnum-id").innerHTML = videocomments))
          : $(`#cmnt_${e}`).show();
      },
    ));
}

function flag() {
  logged
    ? $.get(
        "/ajax/flag.php",
        {
          video: video,
        },
        () => {
          ($(".flags").addClass("inactive"),
            $(".views .dropdown .tooltip .tooltip_right").html("thank you!"));
        },
      )
    : (backtoCookie(window.location.href), (window.location = login_url));
}

function report_comment(e, t) {
  logged
    ? ($.get(
        "/ajax/reportcomment.php",
        {
          comment: e,
        },
        (e) => {
          showtempinfo(t, 3e3, "#F0FAEB");
        },
      ),
      $(`#cmnt_${e}`).hide("slow"))
    : (backtoCookie(window.location.href), (window.location = login_url));
}

function initModals() {
  ($(".first-error").addClass("hide"),
    $(".second-error").addClass("hide"),
    $(".pass-error").addClass("hide"),
    $(".pass-reerror").addClass("hide"),
    $(".reg-error").addClass("hide"),
    (document.getElementById("username").style.cssText =
      "border-color:rgb(255,113,113) "),
    (document.getElementById("modal-email").style.cssText =
      "border-color:rgb(255,113,113) "),
    (document.getElementById("modal-password").style.cssText =
      "border-color:rgb(255,113,113) "),
    (document.getElementById("modal-repassword").style.cssText =
      "border-color:rgb(255,113,113) "));
}

function initregister(e, t, i) {
  (initModals(),
    (1 !== i && "Login in order to view HD videos" !== e) || (relaflog = 1),
    e && $("#popup_content .title").html(e),
    t &&
      (t.status &&
        ($("h3.reg-error").text(t.error),
        $(".reg-error").removeClass("hide"),
        $(".register-modal").css("height", "610")),
      -1 !== ["userfirst", "usersecond"].indexOf(t.field) &&
        (document.getElementById("username").style.cssText =
          "border-color:rgb(255,113,113) !important"),
      -1 !== ["emailfirst", "emailsecond"].indexOf(t.field) &&
        (document.getElementById("modal-email").style.cssText =
          "border-color:rgb(255,113,113) !important"),
      "passfirst" === t.field &&
        (document.getElementById("modal-password").style.cssText =
          "border-color:rgb(255,113,113) !important"),
      "passsecond" === t.field &&
        (document.getElementById("modal-repassword").style.cssText =
          "border-color:rgb(255,113,113) !important")));
}

function showEmbed() {
  const e = renderTemplate("#embedcode");
  (Popup.open(e),
    e.find("#close-edit").on("click", (e) => {
      (e.preventDefault(), Popup.close());
    }),
    e.find("#copycode").on("click", (e) => {
      document.querySelector("#text-embed").select();
      try {
        const e = document.execCommand("copy")
          ? infoSuccessful
          : infoUnsuccessful;
        alert(infoCopCl + ` ${e}`);
      } catch (e) {
        alert(infoOops);
      }
      Popup.close();
    }));
}

function closeEmbed() {
  ($("#embed-modal").remove(), $("body").css("filter", "blur(0px)"));
}

function initLoginReg(e, t) {
  return logged;
}

function showConfirm() {
  (initModals(),
    $("#login-modal").addClass("hide"),
    $("#register-modal").addClass("hide"),
    $("#confirm-modal").removeClass("hide"));
}

function modalSwitch(e) {
  (initModals(),
    e
      ? ($("#login-modal").removeClass("hide"),
        $("#register-modal").addClass("hide"))
      : ($("#register-modal").removeClass("hide"),
        $("#login-modal").addClass("hide")));
}

function initlogin(e, t, i) {
  if (!logged)
    return (
      backtoCookie(window.location.href),
      void (window.location = login_url)
    );
  if (
    ((1 != i && "Login in order to view HD videos" != e) || (relaflog = 1), t)
  ) {
    if ((e && $("#popup_content .title").html(e), t)) {
      const e = JSON.parse(t);
      ($("h3.reg-error").text(e.err),
        $(".reg-error").removeClass("hide"),
        $(".simple-content.login-place").css("height", "480"));
    }
  } else initLoginReg("login", null != video ? video : 0);
}

function trylogin(e) {
  ((e = e || ""), $("#login_loading").show());
  const t = {
      videoid: e,
    },
    i = `${$("#modal-pop-login").serialize()}&${$.param(t, !0)}`;
  $.post("/ajax/login.php", i, (e) => {
    const t = e.split("|");
    if (t[0] > 0) {
      if (
        ((logged = 1),
        "undefined" != typeof relahd &&
          "" !== relahd &&
          localStorage.setItem("quality", relahd),
        "undefined" != typeof relaflog)
      )
        return void window.location.reload();
      (Popup.close(cont),
        $(".authsection").html(
          `<div style="clear: both;"></div><div><div id="user"><a href="/profile"><img src="/images/down-top.png" width="14" height="4"><span class="wrap-profile-img" title="${t[1]}"><span class="cwrap">${t[1]}</span></span><img src="http://avatar.pornone.com/0/0/35x35.jpg" width="35" height="35"></a><div id="open-small-log"><ul><a href="/upload/"><li><img src="/images/my-upload.png" width="16" height="16" alt="">Upload Video</li></a><a href="/user"><li><img src="/images/my-profile.png" width="16" height="16" alt="">My Profile</li></a><a href="/favorite"><li><img src="/images/my-star.png" width="17" height="16" alt="">My Favorites</li></a><a href="/myvideos"><li><img src="/images/my-videos.png" width="16" height="16" alt="">My Videos</li></a><a href="/subscriptions"><li><img src="/images/my-subs.png" width="16" height="16" alt="">My Subscriptions</li></a><a href="/profile"><li><img src="/images/my-settings.png" width="16" height="16" alt="">My Settings</li></a>\n<a href="/logout"><li><img src="/images/my-logout.png" width="16" height="16" alt="">Log out</li></a></ul></div></div><div class="subs-count" data-toggle="tooltip" data-placement="bottom" data-original-title="Subscriptions"><a href="/subscriptions"><img src="/images/my-feed.png" class="notif-p" width="20" height="20" alt=""></a></div></div>`,
        ),
        $("#comment-button").show(),
        $(".comments-area").focus().css("height", "60px"),
        $("#commetn-post").text(""),
        $("#commetn-login").text(""),
        $(".uplbut").attr({
          href: "/upload",
          onclick: "",
        }),
        (subscribed = parseInt(t[3], 10)),
        (uploaderid = parseInt(t[4], 10)));
      const e =
        1 == subscribed
          ? '<div class="info-unsubscribe addhover"><img class="subs-to" width="5" height="9" alt="" src="/images/sub.png"><a class="subscribe-to unsubscribe" href="javascript: subscribe();"><img width="7" height="7" alt="" src="/images/subbed.png"><span class="remove-info">Subscribed</span></a></div>'
          : '<div class="info-unsubscribe"><img class="subs-to" width="5" height="9" alt="" src="/images/subsctibe-to.png"><a class="subscribe-to subscribed" href="javascript: subscribe();"><img width="7" height="7" alt="" src="/images/subs.png"><span>Subscribe</span></a></div>';
      $(".info").after(e);
    } else initlogin(!1, t[1]);
  });
}

function tryregister(e) {
  $.post(
    "/ajax/register.php",
    $("#modal-pop-register").serialize(),
    (t) => {
      if (t.status) initregister(!1, t);
      else {
        if (((logged = 1), "undefined" != typeof relaflog))
          return void (void 0 === e && window.location.reload());
        (Popup.close(cont),
          $("#login-register").css("display", "none"),
          $("#temp-ok").css("display", "block"),
          $(".authsection").html(
            `<div style="clear: both;"></div><div id="user"><a href="/profile"><img src="/images/down-top.png" width="14" height="4"><span class="wrap-profile-img" title="${t.username}"><span class="cwrap">${t.username}</span></span><img src="http://avatar.pornone.com/0/0/35x35.jpg" width="35" height="35"></a><div id="open-small-log">\n<ul><a href="/upload/"><li><img src="/images/my-upload.png" width="16" height="16" alt="">Upload Video</li></a><a href="/user"><li><img src="/images/my-profile.png" width="16" height="16" alt="">My Profile</li></a>\n<a href="/favorite"><li><img src="/images/my-star.png" width="17" height="16" alt="">My Favorites</li></a><a href="/myvideos"><li><img src="/images/my-videos.png" width="16" height="16" alt="">My Videos</li></a><a href="/subscriptions"><li><img src="/images/my-subs.png" width="16" height="16" alt="">My Subscriptions</li></a><a href="/profile"><li><img src="/images/my-settings.png" width="16" height="16" alt="">My Settings</li></a>\n<a href="/logout"><li><img src="/images/my-logout.png" width="16" height="16" alt="">Log out</li></a></ul></div></div><div class="subs-count" data-toggle="tooltip" data-placement="bottom" data-original-title="Subscriptions"><a href="/subscriptions"><img src="/images/my-feed.png" class="notif-p" width="20" height="20" alt=""></a></div></div>`,
          ),
          $("#comment-button").show(),
          $(".comments-area").attr({
            disabled: !1,
          }),
          $(".comments-area").focus(),
          $("#commetn-post").text(""),
          $("#commetn-login").text(""),
          $(".comments-area").css("height", "60px"));
      }
    },
    "json",
  );
}

function knowher(e, t) {
  const i = $(".js-suggest-pornstars").serialize();
  i.length > 0 &&
    $.get(
      "/ajax/videoget.php",
      {
        starapprove: e,
        input: i,
        editstarids: 1,
        lang_id: lang_id,
        lang_name: lang_name,
      },
      (e) => {
        "error" !== e &&
          ($("#suggstardiv").hide(),
          showtempinfo(t, 1e3, "#F0FAEB"),
          $("#suggeststarinp").val(""));
      },
    );
}

function knowcategory(e) {
  const t = $(".js-suggcategory").serialize();
  $.get(
    "/ajax/knowhername.php",
    {
      videoid: video,
      category: t,
      videocats: videocats,
    },
    (t) => {
      "ok" == t && ($("#suggcatdiv").hide(), showtempinfo(e, 1e3, "#F0FAEB"));
    },
  );
}

function knowtag(e) {
  const t = $("#suggestaginp").val();
  t.length > 3 &&
    $.get(
      "/ajax/knowhername.php",
      {
        videoid: video,
        tags: t,
      },
      (t) => {
        "ok" == t &&
          ($("#suggtagdiv").hide(),
          showtempinfo(e, 1e3, "#F0FAEB"),
          $("#suggestaginp").val(""));
      },
    );
}

function rejectag(e) {
  const t = $(`${e} span.tags`).text();
  return (
    t.length > 3 &&
      $.get(
        "/ajax/knowhername.php",
        {
          videoid: video,
          rejecttags: t,
        },
        (e) => {},
      ),
    $(e).popover("title", "Thank you for your help!"),
    $(e).popover("fadeOut"),
    $(e).fadeOut(1e3),
    !1
  );
}

function rejectstar(e) {
  const t = $(`${e} span.tags`).text();
  return (
    t.length > 3 &&
      $.get(
        "/ajax/knowhername.php",
        {
          videoid: video,
          rejectstar: t,
        },
        (e) => {},
      ),
    $(e).popover("title", "Thank you for your help!"),
    $(e).popover("fadeOut"),
    $(e).fadeOut(1e3),
    !1
  );
}

function shareTweeter() {
  window.open(
    "http://twitter.com/share?text=" +
      document.title +
      " -&url=" +
      document.location.href +
      "&hashtags=pornone",
    "new",
  );
}

function rcookie(e) {
  const t = `${e}=`,
    i = document.cookie.split(";");
  for (let e = 0; e < i.length; e++) {
    let s = i[e];
    for (; " " == s.charAt(0); ) s = s.substring(1, s.length);
    if (0 == s.indexOf(t)) return s.substring(t.length, s.length);
  }
  return null;
}

function setcookie(e, t, i) {
  if (i) {
    const e = new Date();
    e.setTime(e.getTime() + 24 * i * 60 * 60 * 1e3);
    var s = `; expires=${e.toGMTString()}`;
  } else s = "";
  document.cookie = `${e}=${t}${s}; path=/;`;
}

function set_storage(e, t) {
  "undefined" != typeof localStorage
    ? localStorage.setItem(e, t)
    : setcookie(e, t, 365);
}

function opencats() {
  const e = $(".catsmenu");
  e.is(":visible")
    ? e.is(":animated") || (e.fadeOut(), $(document).off("click.cats"))
    : (e.fadeIn(),
      $(document).on("click.cats", (e) => {
        $(e.target).parents(".catsmenu").length ||
          $(e.target).hasClass("catsmenu") ||
          opencats();
      }));
}
const onImageChange = function (e) {
  const t = document.getElementById("s_file").files,
    i = t[0],
    s = new FormData();
  let o;
  (s.append("file", i),
    (o = $.ajax({
      url: "/ajax/uploadimg.php",
      type: "POST",
      data: s,
      dataType: "json",
      processData: !1,
      contentType: !1,
      beforeSend() {},
      success(e) {
        (2e3 == e.status ? alert(e.error) : e.result || alert(infoImageUpl),
          window.location.reload());
      },
      error(e) {
        alert(infoERROR);
      },
      xhr() {
        const e = new window.XMLHttpRequest();
        return (
          e.upload &&
            (e.upload.onprogress = function (e) {
              (e.loaded, e.total);
            }),
          e
        );
      },
    })));
};

function formatSize(e, t) {
  return (
    null == t && (t = 1),
    e >= 1099511627776
      ? `${(e / 1099511627776).toFixed(t)} TB`
      : e >= 1073741824
        ? `${(e / 1073741824).toFixed(t)} GB`
        : e >= 1048576
          ? `${(e / 1048576).toFixed(t)} MB`
          : e >= 1024
            ? `${(e / 1024).toFixed(t)} KB`
            : `${e.toFixed(t)} B`
  );
}

function refresh(e, t, i) {
  $.get(
    "/ajax/popular.php",
    {
      maincategory: e,
      mobile: t,
      isrow: i,
      count: listq[15] ? listq[15]++ : (listq[15] = 1),
      lang_name: lang_name,
      lang_id: lang_id,
    },
    (e) => {
      if (e)
        return (
          $(".js-popular").html(e),
          listq[15] > 9 && (listq[15] = 1),
          $(".js-popular img.lazy-loading").each(function () {
            let e = $(this);
            e.attr("src", e.attr("data-src"))
              .removeAttr("data-src")
              .removeClass("opacity-0");
          }),
          !1
        );
    },
  );
}

function vrazkaget(e) {
  for (var t = "", i = 0; i < e.length; i++)
    t += String.fromCharCode(2 ^ e.charCodeAt(i));
  return t;
}

function refresh_related(e, t, i, s) {
  $.get(
    "/ajax/related.php",
    {
      maincategory: s,
      query: e,
      mobile: t,
      isrow: i,
      count: void 0 !== listq[27] ? ++listq[27] : (listq[27] = 1),
      videoid: video,
      lang_id: lang_id,
      lang_name: lang_name,
    },
    (e) => {
      if (e)
        return (
          $(".js-related").append(e),
          $(".js-related img.lazy-loading").each(function () {
            let e = $(this);
            e.attr("src", e.attr("data-src"))
              .removeAttr("data-src")
              .removeClass("opacity-0");
          }),
          t
            ? ($(".imglista").on("touchstart", function () {
                startPreview($(this).attr("id"));
              }),
              $(".imglista").on("touchend", function () {
                stopPreview($(this).attr("id"));
              }))
            : $(".video_wrapper .imgvideo").hover(
                function () {
                  startPreview($(this).attr("id"));
                },
                function () {
                  stopPreview($(this).attr("id"));
                },
              ),
          (window.location =
            window.location.href.split("#")[0] + "#related" + listq[27]),
          listq[27] > 4 && ((listq[27] = 0), $(".loadmore").hide()),
          !1
        );
    },
  );
}

function showHideElmnt(e) {
  const t = $(e);
  void 0 !== t && (t.is(":hidden") ? t.show() : t.hide());
}

function classElmnt(e, t) {
  document.getElementById(e).classList.toggle(t);
}

function isShown(e, t) {
  var i = document.getElementById(e);
  void 0 !== i && null != i && (i.style.display = t);
}

function gosearch(e) {
  ($(window).scrollTop(0),
    refreshsearch(
      maincat,
      ismobile,
      isrow,
      q,
      catsd,
      e,
      pagebase,
      sort,
      filter,
      "undefined" != typeof hd && hd ? "1" : "-1",
    ));
}

function golive(e) {
  ($(window).scrollTop(0),
    refreshlive(
      maincat,
      ismobile,
      isrow,
      q,
      catsd,
      e,
      pagebase,
      region,
      age,
      hd,
      language,
      ethnic,
      body,
      fetish,
      newmod,
    ));
}

function refreshlive(e, t, i, s, o, l, a, n, r, d, c, m, h, p, g) {
  $.get(
    "/ajax/live.php",
    {
      maincategory: e,
      mobile: t,
      isrow: i,
      count: t ? 12 : 30,
      search: s,
      categories: o,
      page: l,
      pagebase: a,
      region: n,
      age: r,
      hd: d,
      language: c,
      ethnic: m,
      body: h,
      fetish: p,
      newmod: g,
    },
    (e) => {
      if (e) {
        ((location.search.indexOf("q=") >= 0 ||
          location.search.indexOf("cats=") >= 0) &&
          $(".pages").hide(),
          $(".js-category").data("select2") ||
            $(".js-category").select2({
              placeholder: "Filter by Category...",
            }),
          $("#js-videos").html(e),
          isShown("cats-show", "none"),
          isShown("videos-show", "block"),
          $(".livejasmin-videos-heading").hide(),
          $(".livejasmin-videos-container").hide());
        var t = "";
        return (
          ismobile
            ? $(".nav-videos a").each(function (e, i) {
                void 0 !== $(this).attr("href") &&
                  ((t = $(this).attr("href")).indexOf("?") > 0
                    ? $(this).attr(
                        "href",
                        t.substring(0, t.indexOf("?")) +
                          ("" !== s
                            ? "?q=" +
                              s +
                              "&cats=" +
                              encodeURIComponent(JSON.stringify(catsd))
                            : "?cats=" +
                              encodeURIComponent(JSON.stringify(catsd))),
                      )
                    : $(this).attr(
                        "href",
                        ("//" === t.substring(t.length - 2)
                          ? t.slice(0, -1)
                          : t) +
                          ("" !== s
                            ? "?q=" +
                              s +
                              "&cats=" +
                              encodeURIComponent(JSON.stringify(catsd))
                            : "?cats=" +
                              encodeURIComponent(JSON.stringify(catsd))),
                      ));
              })
            : $(".pages a").each(function (e, i) {
                void 0 !== $(this).attr("href") &&
                  ((t = $(this).attr("href")).indexOf("?") > 0
                    ? $(this).attr(
                        "href",
                        t.substring(0, t.indexOf("?")) +
                          ("" !== s
                            ? "?q=" +
                              s +
                              "&cats=" +
                              encodeURIComponent(JSON.stringify(catsd))
                            : "?cats=" +
                              encodeURIComponent(JSON.stringify(catsd))),
                      )
                    : $(this).attr(
                        "href",
                        ("//" === t.substring(t.length - 2)
                          ? t.slice(0, -1)
                          : t) +
                          ("" !== s
                            ? "?q=" +
                              s +
                              "&cats=" +
                              encodeURIComponent(JSON.stringify(catsd))
                            : "?cats=" +
                              encodeURIComponent(JSON.stringify(catsd))),
                      ));
              }),
          $(".heading-1.catselect").show(),
          !1
        );
      }
    },
  );
}

function refreshsearch(e, t, i, s, o, l, a, n, r, d) {
  $.get(
    "/ajax/search.php",
    {
      maincategory: e,
      mobile: t,
      isrow: i,
      count: 48,
      search: s,
      categories: o,
      page: l,
      pagebase: a,
      sort: n,
      filter: r,
      hd: d,
      isuser: isuser,
      isquoted: isquoted,
      lang_id: lang_id,
      lang_name: lang_name,
    },
    (e) => {
      if (e) {
        ($(".js-category").data("select2") ||
          $(".js-category").select2({
            placeholder: "Filter by Category...",
          }),
          $("#js-videos").html(e),
          isShown("cats-show", "none"),
          isShown("videos-show", "block"),
          $(".js-category").val(catsd),
          $(".js-category").trigger("change"));
        var t = encodeURIComponent(JSON.stringify(catsd)),
          i =
            window.location.protocol +
            "//" +
            window.location.host +
            window.location.pathname +
            ("" !== s
              ? "?q=" +
                s +
                "&sort=" +
                n +
                "&filter=" +
                r +
                "&page=" +
                l +
                "&cats=" +
                t.replace("'", "")
              : "?cats=" +
                t.replace("'", "") +
                "&sort=" +
                n +
                "&filter=" +
                r +
                "&page=" +
                l);
        (window.history.pushState(
          {
            path: i,
          },
          "",
          i,
        ),
          (state =
            "#" == window.location.href.charAt(window.location.href.length - 1)
              ? window.location.href.slice(0, -1)
              : window.location.href));
        var o = "";
        return (
          ismobile
            ? $(".nav-videos a").each(function (e, t) {
                void 0 !== $(this).attr("href") &&
                  ((o = $(this).attr("href")).indexOf("?") > 0
                    ? $(this).attr(
                        "href",
                        o.substring(0, o.indexOf("?")) +
                          ("" !== s
                            ? "?q=" +
                              s +
                              "&cats=" +
                              encodeURIComponent(JSON.stringify(catsd))
                            : "?cats=" +
                              encodeURIComponent(JSON.stringify(catsd))),
                      )
                    : $(this).attr(
                        "href",
                        ("//" === o.substring(o.length - 2)
                          ? o.slice(0, -1)
                          : o) +
                          ("" !== s
                            ? "?q=" +
                              s +
                              "&cats=" +
                              encodeURIComponent(JSON.stringify(catsd))
                            : "?cats=" +
                              encodeURIComponent(JSON.stringify(catsd))),
                      ));
              })
            : $(".pages a").each(function (e, t) {
                void 0 !== $(this).attr("href") &&
                  ((o = $(this).attr("href")).indexOf("?") > 0
                    ? $(this).attr(
                        "href",
                        o.substring(0, o.indexOf("?")) +
                          ("" !== s
                            ? "?q=" +
                              s +
                              "&cats=" +
                              encodeURIComponent(JSON.stringify(catsd))
                            : "?cats=" +
                              encodeURIComponent(JSON.stringify(catsd))),
                      )
                    : $(this).attr(
                        "href",
                        ("//" === o.substring(o.length - 2)
                          ? o.slice(0, -1)
                          : o) +
                          ("" !== s
                            ? "?q=" +
                              s +
                              "&cats=" +
                              encodeURIComponent(JSON.stringify(catsd))
                            : "?cats=" +
                              encodeURIComponent(JSON.stringify(catsd))),
                      ));
              }),
          ismobile
            ? ($(".imglista").on("touchstart", function () {
                startPreview($(this).attr("id"));
              }),
              $(".imglista").on("touchend", function () {
                stopPreview($(this).attr("id"));
              }))
            : $(".video_wrapper .imgvideo").hover(
                function () {
                  startPreview($(this).attr("id"));
                },
                function () {
                  stopPreview($(this).attr("id"));
                },
              ),
          location.search.indexOf("q=") >= 0 &&
            $(".js-category").next(".select2-container").show(),
          $("img.lazy-loading").each(function () {
            let e = $(this);
            e.attr("src", e.attr("data-src"))
              .removeAttr("data-src")
              .removeClass("opacity-0");
          }),
          $(".heading-1.catselect").show(),
          !1
        );
      }
    },
  );
}

function closeCB() {
  (setcookie("cookiewarning", !0, 15),
    (document.querySelector(".cbann").style.display = "none"));
}

function setvideonext(e) {
  if ("undefined" != typeof next && (!1 !== next || void 0 !== e)) {
    const t = e || next;
    window.videojs &&
      !0 !== t &&
      (setTimeout(() => {
        videojs("pornone-video-player_html5_api")
          .getChild("controlBar")
          .addChild(
            "NextButton",
            {
              name: "nextButton",
              next: t,
              text: "",
            },
            2,
          );
      }, 20),
      (next = void 0));
  }
}
("requestIdleCallback" in window ||
  (window.requestIdleCallback = function (e) {
    return setTimeout(e, 16);
  }),
  ("scheduler" in window && "postTask" in scheduler) ||
    (window.scheduler = {
      postTask: function (e, t) {
        return t && "user-blocking" === t.priority
          ? setTimeout(e, 0)
          : setTimeout(e, 16);
      },
    }));
const iamover18Btn = document.getElementById("iamover18Btn"),
  modal = document.getElementById("embed-modal");
(iamover18Btn &&
  modal &&
  iamover18Btn.addEventListener(
    "click",
    function () {
      (modal.classList.add("hiding"),
        setTimeout(() => {
          (setcookie("over_18", 1, 365), modal.remove());
        }, 150));
    },
    {
      passive: !0,
      once: !0,
    },
  ),
  $(document).ready(() => {
    if (
      (changeCSSimages(
        "dark-theme" == theme.replace("small", "theme") ? "-dark" : "",
      ),
      "dark-theme" == theme && !ismobile)
    ) {
      var e = document.getElementById("live_sex_cam");
      null !== e && (e.src = livesexcamd_url);
    }
    if (
      ($("a.setH").each(function () {
        let e = $(this);
        e.attr("href", e.data("href")).removeAttr("data-href");
      }),
      document.querySelectorAll("a").forEach(function (e) {
        e.addEventListener("click", function (e) {
          var t = this.getAttribute("data-thparam"),
            i = "thparam";
          if (
            (t || ((t = this.getAttribute("data-backto")), (i = "backto")),
            t || ((t = this.getAttribute("data-report")), (i = "report")),
            t)
          ) {
            const e = new Date(Date.now() + 18e4);
            document.cookie = i + "=" + t + `; expires=${e}; path=/`;
          }
        });
      }),
      $("img.src-set").each(function () {
        let e = $(this);
        e.attr("srcset", e.data("srcset")).removeAttr("data-srcset");
      }),
      notlost ||
        ($(".pldropdownmaster").on("click", function (e) {
          (e.preventDefault(), e.stopPropagation());
          var t = $(this).find(".pldropdown");
          t.hasClass("hidMenu")
            ? (t.css("visibility", "hidden"), t.removeClass("hidMenu"))
            : (t.css("visibility", "visible"), t.addClass("hidMenu"));
        }),
        $(".logon").on("click", function (e) {
          (e.preventDefault(),
            e.stopPropagation(),
            (window.location.href = "/login"));
        })),
      $("body").click(function (e) {
        $(".skrit").hasClass("hidMenu") &&
          (e.preventDefault(),
          $(".skrit").css("visibility", "hidden"),
          $(".skrit").removeClass("hidMenu"));
      }),
      ismobile)
    ) {
      ($(".gandermenu").on("click", function (e) {
        e.stopPropagation();
        var t = $(this).find(".skrit");
        t.hasClass("hidMenu")
          ? (t.css("visibility", "hidden"), t.removeClass("hidMenu"))
          : (t.css("visibility", "visible"), t.addClass("hidMenu"));
      }),
        $("#lMenu,#searchModal").on(
          "scroll touchmove mousewheel",
          function (e) {
            return (e.preventDefault(), e.stopPropagation(), !1);
          },
        ));
      const e = $("#leftmenu");
      ($(".cat ,.menu-top-left").on("click", function (t) {
        (event.stopPropagation(),
          "hidden" === e.css("visibility")
            ? e.css("visibility", "visible")
            : e.css("visibility", "hidden"));
      }),
        e.click(function (e) {
          e.stopPropagation();
        }),
        window.addEventListener("mouseup", function (t) {
          "visible" === e.css("visibility") &&
            $(t.target).parents("#leftmenu").length < 1 &&
            "Categories" !== t.target.alt &&
            t.target != e &&
            t.target.parentNode != e &&
            e.css("visibility", "hidden");
        }),
        window.addEventListener("touchstart", function (t) {
          "visible" === e.css("visibility") &&
            $(t.target).parents("#leftmenu").length < 1 &&
            "Categories" !== t.target.alt &&
            t.target != e &&
            t.target.parentNode != e &&
            e.css("visibility", "hidden");
        }));
    }
    const t = document.getElementById("search-btn"),
      i = document.getElementById("search-input");
    let s = !1;
    if (t && i) {
      const e = t.getAttribute("data-path") || "/search";
      ((t.onclick = (o) => {
        (o.preventDefault(),
          s
            ? i.value.trim()
              ? (location.href =
                  e +
                  "?q=" +
                  encodeURIComponent(
                    i.value.trim().toLowerCase(),
                  ).toLowerCase())
              : i.focus()
            : (i.classList.add("open"),
              t.classList.add("open"),
              (s = !0),
              i.focus()));
      }),
        (i.onkeydown = (t) => {
          "Enter" === t.key &&
            i.value.trim() &&
            (location.href =
              e +
              "?q=" +
              encodeURIComponent(i.value.trim().toLowerCase()).toLowerCase());
        }),
        (document.onclick = (e) => {
          s &&
            !e.target.closest("#search-container") &&
            (i.classList.remove("open"),
            t.classList.remove("open"),
            (s = !1),
            (i.value = ""));
        }));
    }
    ($("#playlistButton").on("click", function (e) {
      ($("#playlistModal").show(),
        playtab(
          new URLSearchParams(window.location.href.split("?")[1]).get("que")
            ? "tabqueue"
            : "tabplaylist",
        ));
    }),
      location.search.indexOf("consucs=") >= 0 &&
        (showEmbed(),
        setTimeout(function () {
          closeEmbed();
        }, 2e3)),
      location.search.indexOf("cats=") >= 0 &&
        ($(".hov-menu span").each(function (e, t) {
          $(this).attr("onclick").indexOf("?") > 0
            ? $(this).attr(
                "onclick",
                $(this)
                  .attr("onclick")
                  .substring(0, $(this).attr("onclick").indexOf("?")) +
                  ("" !== q
                    ? "?q=" +
                      q +
                      "&cats=" +
                      encodeURIComponent(JSON.stringify(catsd)) +
                      "'"
                    : "?cats=" +
                      encodeURIComponent(JSON.stringify(catsd)) +
                      "'"),
              )
            : $(this).attr(
                "onclick",
                $(this).attr("onclick").slice(0, -1) +
                  ("" !== q
                    ? "?q=" +
                      q +
                      "&cats=" +
                      encodeURIComponent(JSON.stringify(catsd)) +
                      "'"
                    : "?cats=" +
                      encodeURIComponent(JSON.stringify(catsd)) +
                      "'"),
              );
        }),
        ismobile
          ? $(".hd-button span").each(function (e, t) {
              $(this).attr("onclick").indexOf("?") > 0
                ? $(this).attr(
                    "onclick",
                    $(this)
                      .attr("onclick")
                      .substring(0, $(this).attr("onclick").indexOf("?")) +
                      ("" !== q
                        ? "?q=" +
                          q +
                          "&cats=" +
                          encodeURIComponent(JSON.stringify(catsd)) +
                          "'"
                        : "?cats=" +
                          encodeURIComponent(JSON.stringify(catsd)) +
                          "'"),
                  )
                : $(this).attr(
                    "onclick",
                    $(this).attr("onclick").slice(0, -1) +
                      ("" !== q
                        ? "?q=" +
                          q +
                          "&cats=" +
                          encodeURIComponent(JSON.stringify(catsd)) +
                          "'"
                        : "?cats=" +
                          encodeURIComponent(JSON.stringify(catsd)) +
                          "'"),
                  );
            })
          : $(".hd-filter span").each(function (e, t) {
              $(this).attr("onclick").indexOf("?") > 0
                ? $(this).attr(
                    "onclick",
                    $(this)
                      .attr("onclick")
                      .substring(0, $(this).attr("onclick").indexOf("?")) +
                      ("" !== q
                        ? "?q=" +
                          q +
                          "&cats=" +
                          encodeURIComponent(JSON.stringify(catsd)) +
                          "'"
                        : "?cats=" +
                          encodeURIComponent(JSON.stringify(catsd)) +
                          "'"),
                  )
                : $(this).attr(
                    "onclick",
                    $(this).attr("onclick").slice(0, -1) +
                      ("" !== q
                        ? "?q=" +
                          q +
                          "&cats=" +
                          encodeURIComponent(JSON.stringify(catsd)) +
                          "'"
                        : "?cats=" +
                          encodeURIComponent(JSON.stringify(catsd)) +
                          "'"),
                  );
            }),
        ismobile
          ? $(".nav-videos a").each(function (e, t) {
              void 0 !== $(this).attr("href") &&
                ($(this).attr("href").indexOf("?") > 0
                  ? $(this).attr(
                      "href",
                      $(this)
                        .attr("href")
                        .substring(0, $(this).attr("href").indexOf("?")) +
                        ("" !== q
                          ? "?q=" +
                            q +
                            "&cats=" +
                            encodeURIComponent(JSON.stringify(catsd))
                          : "?cats=" +
                            encodeURIComponent(JSON.stringify(catsd))),
                    )
                  : $(this).attr(
                      "href",
                      $(this).attr("href").slice(0, -1) +
                        ("" !== q
                          ? "?q=" +
                            q +
                            "&cats=" +
                            encodeURIComponent(JSON.stringify(catsd))
                          : "?cats=" +
                            encodeURIComponent(JSON.stringify(catsd))),
                    ));
            })
          : $(".pages a").each(function (e, t) {
              void 0 !== $(this).attr("href") &&
                ($(this).attr("href").indexOf("?") > 0
                  ? $(this).attr(
                      "href",
                      $(this)
                        .attr("href")
                        .substring(0, $(this).attr("href").indexOf("?")) +
                        ("" !== q
                          ? "?q=" +
                            q +
                            "&cats=" +
                            encodeURIComponent(JSON.stringify(catsd))
                          : "?cats=" +
                            encodeURIComponent(JSON.stringify(catsd))),
                    )
                  : $(this).attr(
                      "href",
                      $(this).attr("href").slice(0, -1) +
                        ("" !== q
                          ? "?q=" +
                            q +
                            "&cats=" +
                            encodeURIComponent(JSON.stringify(catsd))
                          : "?cats=" +
                            encodeURIComponent(JSON.stringify(catsd))),
                    ));
            })),
      $("a.cwrap, .thumb-info").hover(
        function () {
          $(this).toggleClass("full");
        },
        function () {
          $(this).toggleClass("full");
        },
      ),
      $("ul.dropdown li").hover(
        function () {
          ($(this).addClass("hover"),
            $("ul:first", this).css("visibility", "visible"));
        },
        function () {
          ($(this).removeClass("hover"),
            $("ul:first", this).css("visibility", "hidden"));
        },
      ),
      $("[title]").on("mouseenter", (e) => {
        e.preventDefault();
      }),
      window.listq &&
        ((listq[13] = 1),
        (listq[15] = 1),
        (!listq[0] && !listq[1]) || ismobile || showplaylist()),
      $(".playright").on("click", (e) => {
        showplaylist();
      }),
      $("#edit-photo").click(() => {
        document.getElementById("s_file").click();
      }),
      $("#s_file").change(() => {
        onImageChange();
      }),
      "/" == window.location.pathname &&
        (document.cookie = "duration= ; path=/"));
    const o = {
      minChars: 2,
      onSelect: (e) => (
        document.getElementById("search-form-sub").submit(),
        !1
      ),
      lookup(e, t) {},
    };
    if (
      "undefined" != typeof srr &&
      0 !== srr.length &&
      $("#search-inp-desk").val().length < 3
    ) {
      let e = !1;
      ($("#search-inp-desk").devbridgeAutocomplete({
        lookup: srr,
        minChars: 0,
        onSelect(e) {
          if ($("#search-inp-desk").val().length > 3)
            return (document.getElementById("search-form-sub").submit(), !1);
        },
      }),
        $("#search-inp-desk").on("keyup", function () {
          e ||
            2 != this.value.length ||
            ((e = !0), $("#search-inp-desk").autocomplete("setOptions", o));
        }));
    } else $("#search-inp-desk").devbridgeAutocomplete(o);
    (fillqueuelist(),
      "undefined" != typeof lists &&
        (movescrollpadding(Object.keys(lists).length, "#play_list"),
        listq[3] &&
          (movescrollpadding(lists[listq[3]].status, "#play_listlist"),
          listq[0] &&
            lists[listq[3]].status > 6 &&
            scrolltoclass("#play_listlist", ".selektplistlist"))),
      ($.event.special.touchstart = {
        setup(e, t, i) {
          ismobile &&
            void 0 !== t &&
            t &&
            "contextMenu1" === t[0] &&
            this.addEventListener("touchstart", i, {
              passive: !1,
            });
        },
      }),
      notlost &&
        $.contextMenu({
          selector: ".context-menu",
          zIndex: 101,
          trigger: ismobile ? "touchstart" : "left",
          autoHide: !1,
          hideOnSecondTrigger: !1,
          build(e, t) {
            return {
              callback(e, t) {
                if (
                  ((listq[11] = t.$trigger.attr("ids")),
                  (listq[12] = t.$trigger.attr("thumb")),
                  "id" == e.substring(0, 2))
                ) {
                  const i = parseInt(e.substring(2));
                  lists[i].status >= 1e3
                    ? showtempinfo(infoCantAdd, 3e3, !1)
                    : null === lists[i].list ||
                        -1 == lists[i].list.indexOf(t.$trigger.attr("ids"))
                      ? ((lists[i].list += `,${t.$trigger.attr("ids")}`),
                        $.get(
                          "/ajax/videoget.php",
                          {
                            playup: t.$trigger.attr("ids"),
                            playid: lists[i].id,
                          },
                          (t) => {
                            if ("error" != t) {
                              $(`#listimg${lists[i].id}`).attr(
                                "src",
                                `${thumbpath}t/${listq[11] % 100}/${listq[11]}/d${listq[12]}.jpg`,
                              );
                              const t = $(`#listcount${lists[i].id}`);
                              (lists[lists[i].id].status++,
                                t.html(`${lists[lists[i].id].status} videos`),
                                showtempinfo(
                                  infoAddedTo +
                                    ` ${lists[e.substring(2)].title}`,
                                  3e3,
                                  "#F0FAEB",
                                ));
                            }
                          },
                        ))
                      : showtempinfo(
                          infoAlready + ` ${lists[e.substring(2)].title}`,
                          3e3,
                          !1,
                        );
                }
                if (
                  ("Queue" == e && addtoqueue(t.$trigger.attr("ids")),
                  "Favorites" == e)
                ) {
                  if (!logged)
                    return (
                      backtoCookie(window.location.href),
                      (window.location = login_url)
                    );
                  ((i = t.$trigger.attr("ids")),
                    "undefined" != typeof vafids
                      ? vafids.includes(i)
                        ? (favorite(i, 1),
                          (vafids = vafids.replace(`,${i}`, "")))
                        : ((vafids += `,${i}`), favorite(i, 0))
                      : favorite(i, 0));
                }
                var i;
                if ("NewPlaylist" == e)
                  return (
                    !logged &&
                    (backtoCookie(window.location.href),
                    (window.location = login_url))
                  );
              },
              items: {
                Queue: {
                  name: infoAddQueue,
                },
                Favorites: {
                  name:
                    ((i = e.attr("ids")),
                    "undefined" != typeof vafids && vafids.includes(i)
                      ? infoDelFavs
                      : infoAddFavs),
                },
                Playlist: {
                  name: infoAddPlayl,
                  items: loadItems(),
                },
              },
            };
            var i;
          },
        }),
      $(document).on("keyup", "#newplaylistname", () => {
        (13 === event.keyCode && newplaylist(),
          27 === event.keyCode && $(".context-menu-active").contextMenu("hide"),
          32 === event.keyCode &&
            $("#newplaylistname").val(`${$("#newplaylistname").val()} `));
      }),
      $(document).on("click", ".vrazka", function () {
        return (window.open(vrazkaget($(this).data("vrazka")), "_blank"), !1);
      }),
      $(document).on("click", ".vrazkaself", function () {
        return (window.open(vrazkaget($(this).data("vrazka")), "_self"), !1);
      }),
      $("[data-toggle=tooltip]").tooltip());
  }));
const ChangeTheme = (() => {
  const e = ".js-change-theme",
    t = ["dark-theme", "light-theme"];

  function i() {
    const e = localStorage ? localStorage.getItem("theme") : "dark-theme",
      i = t.find((t) => t !== e);
    var s;
    (localStorage && localStorage.setItem("theme", i),
    $("body").removeClass(t.join(" ")).addClass(i),
    window.changeCSSimages("dark-theme" === i ? "-dark" : ""),
    "dark-theme" === i)
      ? (document.documentElement.classList.add("dark"),
        ismobile ||
          (null !== (s = document.getElementById("live_sex_cam")) &&
            (s.src = livesexcamd_url)))
      : (document.documentElement.classList.remove("dark"),
        ismobile ||
          (null !== (s = document.getElementById("live_sex_cam")) &&
            (s.src = livesexcam_url)));
  }
  return {
    wrapFor: e,
    initialize: function () {
      $(e).on("click", i);
    },
  };
})();
(window.App.modules.push(ChangeTheme),
  $(document).ready(() => {
    App.initialize();
  }));
